# perpetual-scholar-agent — Requirements
# Install: pip install -r requirements.txt
# (Or use: poetry install for dev environment)

# Core ML / DL
torch>=2.3
transformers>=4.42
peft>=0.11
trl>=0.9
sentence-transformers>=3.0
stable-baselines3>=2.3
gymnasium>=0.29
faiss-cpu>=1.8

# Ingestion
crawl4ai>=0.4
arxiv>=2.1
PyGithub>=2.3

# Execution / Infrastructure
docker>=7.0
aiosqlite>=0.20
apscheduler>=3.10
httpx>=0.27

# Web / API
fastapi>=0.111
uvicorn>=0.30
pydantic-settings>=2.3
python-dotenv>=1.0

# Optional LLM backends
anthropic>=0.29
openai>=1.35

# Monitoring / Logging
structlog>=24.1
tenacity>=8.3
prometheus-client>=0.20

# Testing
pytest>=8.2
pytest-benchmark>=4.0
pytest-cov>=5.0
pytest-asyncio>=0.23
numpy>=1.26
